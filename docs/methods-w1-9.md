# W1.9 methods note

Stub — W1.9 (protected-path policy diff check, D11) is in progress. This note will record the
design of the base-ref policy diff check, its composition with the shared protected-path
taxonomy and the no-shell git invocation, and the `CQ_MERGE_PROTECTED_PATHS` opt-in.
