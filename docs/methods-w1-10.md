# W1.10 methods note

Stub — W1.10 (privileged-job provenance and rulesets-as-code) is in progress. This note will
record how the I2 acceptance check runs as a required status per ADR-0004, the branch
protection as code with its drift check (D13), the promotion job's trigger verification per
P1, the PROMOTE_TOKEN re-scope, the protected environments, and the template render-diff test.
